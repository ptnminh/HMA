export enum ROLES {
  ADMIN = 1,
  Manager = 2,
  Doctor = 3,
  USER = 4,
}

export enum EVENTS {
  AUTH_REGISTER = 'send_email_register',
}


export enum EVENTS {
  AUTH_RESET_PASSWORD = 'send_email_reset_password'
}